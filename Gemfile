# frozen_string_literal: true

source 'https://rubygems.org'

# required to do jekyll
gem 'jekyll'

# adds last modified date as a page variable
gem 'jekyll-last-modified-at'

# required to run locally
gem 'webrick'
